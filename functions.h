#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <iostream>
#include<math.h>
using namespace std;

int stooi(string text);
float stoof(string text);
string nospace(string mystring);
string RSHash(string str );

#endif